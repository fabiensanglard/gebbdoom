bool P_PathTraverse (fixed_t x1, fixed_t y1, // origin
                     fixed_t x2, fixed_t y2, // dest
                     int flags,              // targets
                     boolean (*trav) (intercept_t *));
